# Environment configuration for Jira Analytics Dashboard
# Copy this file to .env and update with your values

# Server Configuration
NODE_ENV=production
PORT=3001

# CORS Configuration (for intranet deployment)
# Comma-separated list of allowed origins
# Example: CORS_ORIGIN=http://intranet.company.com,https://analytics.company.local
CORS_ORIGIN=*

# Application Settings
# Maximum number of tickets to fetch per batch
BATCH_SIZE=100

# Session Configuration
SESSION_SECRET=your-secret-key-here-change-in-production

# Logging Level (error, warn, info, debug)
LOG_LEVEL=info

# Optional: External Jira URL if different from configured
# JIRA_BASE_URL=https://your-jira-instance.atlassian.net

# Optional: Proxy Configuration for corporate networks
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080
# NO_PROXY=localhost,127.0.0.1,.company.local

# Optional: TLS/SSL Configuration
# NODE_TLS_REJECT_UNAUTHORIZED=0  # Only for self-signed certificates in dev